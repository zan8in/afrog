id: redis-weak-login

info:
  name: Redis Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    Redis weak credentials or unauthorized access were discovered (dictionary brute-force in go).
  tags: network,redis,default-login,weak-login
  # requires: [redis]
  created: 2026/01/17

rules:
  r0:
    request:
      type: go
      data: redis-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
