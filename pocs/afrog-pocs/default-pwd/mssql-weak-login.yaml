id: mssql-weak-login

info:
  name: MSSQL Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    MSSQL weak credentials were discovered (dictionary brute-force in go).
  tags: network,mssql,default-login,weak-login
  requires: [mssql]
  created: 2026/01/17

rules:
  r0:
    request:
      type: go
      data: mssql-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
