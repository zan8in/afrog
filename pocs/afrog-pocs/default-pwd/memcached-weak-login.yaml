id: memcached-weak-login

info:
  name: Memcached Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    Memcached weak credentials or unauthorized access were discovered (dictionary brute-force in go).
  tags: network,memcached,default-login,weak-login
  # requires: [memcached]
  created: 2026/01/16

rules:
  r0:
    request:
      type: go
      data: memcached-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
