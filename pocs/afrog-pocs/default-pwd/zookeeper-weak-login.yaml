id: zookeeper-weak-login

info:
  name: ZooKeeper Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    ZooKeeper digest weak credentials were discovered (dictionary brute-force in go).
  tags: network,zookeeper,default-login,weak-login
  # requires: [zookeeper]
  created: 2026/01/18

rules:
  r0:
    request:
      type: go
      data: zookeeper-digest-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
