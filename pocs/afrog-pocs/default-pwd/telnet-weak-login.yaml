id: telnet-weak-login

info:
  name: Telnet Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    Telnet weak credentials were discovered (dictionary brute-force in go).
  tags: network,telnet,default-login,weak-login
  requires: [telnet]
  created: 2026/01/18

rules:
  r0:
    request:
      type: go
      data: telnet-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
