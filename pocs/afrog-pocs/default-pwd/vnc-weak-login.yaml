id: vnc-weak-login

info:
  name: VNC Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    VNC weak credentials or unauthorized access were discovered (dictionary brute-force in go).
  tags: network,vnc,default-login,weak-login
  requires: [vnc]
  created: 2026/01/18

rules:
  r0:
    request:
      type: go
      data: vnc-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
