id: imap-weak-login

info:
  name: IMAP Weak Login
  author: zan8in
  severity: high
  verified: true
  description: |-
    IMAP weak credentials were discovered (dictionary brute-force in go).
  tags: network,imap,default-login,weak-login
  requires: [imap]
  created: 2026/01/16

rules:
  r0:
    request:
      type: go
      data: imap-weak-login
    expression: response.raw.bcontains(b"success;")
    extractors:
      - type: regex
        extractor:
          ext: '"user=(?P<username>[^;]*);pass=(?P<password>.*)$".bsubmatch(response.raw)'
          username: ext["username"]
          password: ext["password"]

expression: r0()
