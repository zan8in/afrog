id: zookeeper-detect

info:
  name: ZooKeeper Service - Detect
  author: zan8in
  severity: info
  verified: true
  description: |-
    ZooKeeper service was detected.
  tags: network,zookeeper,fingerprint
  created: 2026/01/09

set:
  host: request.url.host
rules:
  r0:
    request:
      type: tcp
      host: "{{host}}"
      data: "ruok\n"
    expression: response.raw.bcontains(b"imok")
expression: r0()
