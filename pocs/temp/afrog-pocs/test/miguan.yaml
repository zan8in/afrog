id: miguan

info:
  name: 蜜罐
  author: zan8in
  severity: critical
  verified: true

rules:
  r0:
    request:
      method: GET
      path: /
    expression: |
      response.body.bcontains(b"You Know, for Search") && 
      response.body.bcontains(b"D-Link") && 
      "root:.*?:[0-9]*:[0-9]*:".bmatches(response.body)
expression: r0()
