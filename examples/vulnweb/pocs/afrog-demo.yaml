id: afrog-demo

info:
  name: afrog demo detect
  author: zan8in
  severity: info
  tags: afrog-demo,fingerprint

rules:
  r0:
    request:
      method: GET
      path: /
      follow_redirects: true
    expression: response.body.ibcontains(b'afrog demo')
expression: r0()
