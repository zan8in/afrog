id: fileupload-test

info:
  name: fileupload test
  author: zan8in
  severity: high

rules:
  r0:
    request:
      method: GET
      path: /fileupload
      follow_redirects: true
    expression: response.body.ibcontains(b'fileupload test')
expression: r0()
