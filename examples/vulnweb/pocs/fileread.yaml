id: fileread

info:
  name: fileread test
  author: zan8in
  severity: high
  tags: fileread
  requires: [nacos]
  requires-mode: opportunistic

rules:
  r0:
    request:
      method: GET
      path: /fileread
      follow_redirects: true
    expression: response.body.ibcontains(b'fileread test')
expression: r0()
